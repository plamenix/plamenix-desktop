{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for the main and splash windows.",
  "windows": ["main", "splash"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:window:default"
  ]
}
